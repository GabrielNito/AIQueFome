import EditarCombo from "@/components/dashboard/gerenciamento-de-combos/EditarCombo";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Editar Combo",
};

export default function EditComboPage() {
  return <EditarCombo />;
}
